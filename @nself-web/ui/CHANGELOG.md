# Changelog — @nself-web/ui

## [0.1.0] — 2026-05-15

### Added

- Initial publish of shared UI component library for nSelf web marketing sites
- tsup build pipeline producing ESM (`.mjs`), CJS (`.js`), and type declarations (`.d.ts`)
- Components: Button, Pill, Badge, Card, Header, Footer, StatusPill, Logo, ThemeToggle, Layout, LandingHero
- Utility: `cn` (clsx + tailwind-merge)
- Peer dependencies: react ^18 || ^19, react-dom ^18 || ^19
