{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "@nself/tsconfig/node — strict base for Node/server surfaces (no DOM lib).",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  }
}
