{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "@nself/tsconfig/react — strict base + DOM lib + react-jsx, for React/Vite + React Native surfaces.",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  }
}
