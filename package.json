{
  "name": "nself-packages",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "typecheck": "tsc --noEmit --project tsconfig.base.json",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
